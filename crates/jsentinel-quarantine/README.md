# jsentinel-quarantine

Future quarantine and restore boundary.

Package 0 intentionally does not move, delete, lock, or restore real files. The v1 direction is reversible quarantine and restore, not irreversible deletion.
