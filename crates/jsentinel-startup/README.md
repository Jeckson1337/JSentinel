# jsentinel-startup

Startup visibility boundary.

This crate currently models startup entries only. It does not read or write the Windows registry, systemd units, startup folders, or desktop entries.
