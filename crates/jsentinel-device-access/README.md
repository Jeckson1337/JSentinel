# jsentinel-device-access

Device access visibility boundary.

This crate will eventually model local camera, microphone, location, USB, and related access events where the OS exposes safe APIs. Package 0 does not request permissions or inspect real device usage.
