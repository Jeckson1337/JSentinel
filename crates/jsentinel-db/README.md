# jsentinel-db

Local database boundary for JSentinel.

Future implementations should keep data on the user's machine, expose clear retention controls, and avoid account identifiers, telemetry identifiers, or remote sync by default.
