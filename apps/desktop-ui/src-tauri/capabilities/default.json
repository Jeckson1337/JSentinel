{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default JSentinel desktop UI capability. No privileged JSentinel actions are exposed in Package 0.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
