{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default JSentinel desktop UI capability. Package 3 exposes read-only JSentinel commands only; no privileged actions are exposed.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
